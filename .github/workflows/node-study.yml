name: nodejs-github-actions-study
on:
  push:
    branches:
      - master
jobs:
  test-node:
    runs-on: ubuntu-latest
    env:
      NODE_AUTH_TOKEN_EXAMPLE: ${{ secrets.MY_TEST_SECRET }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.19.0'
      - run: echo "$(node -v)"
      - run: echo "my test secret： ${{ env.NODE_AUTH_TOKEN_EXAMPLE }}"
      - name: List files in the repository
        run: |
          ls
